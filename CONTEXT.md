# CONTEXT — 도메인 용어집

이 repo의 정본 용어 — 코드·문서·대화에서 같은 뜻으로 쓴다. 상세는 `docs/domain/` 각 문서(지도: `docs/domain/README.md`), 되돌리기 어려운 결정은 `docs/adr/`.

## 신호 (언제)

| 용어 | 정의 |
|---|---|
| **모델** (현행) | 진입 판정기 — LightGBM 800트리(5분봉 Feature 33개, 삼중장벽 라벨 +5%/−2%/120분 first-touch). "지금 사면 −2% 찍기 전에 +5%에 닿을 확률"을 내고 **임계값**을 넘으면 BUY. **청산 신호는 내지 않는다**(진입 전용 분류기). 봉 마감마다 트레이딩 리스트 **전 종목**을 1회 판정 — 판정 주체는 `features/scalper/modelScanner.ts`이고 슬롯은 신호를 흘리기만 한다. 필터는 학습·백테스트가 걸어 둔 것 그대로: 정규장 봉 · 그날 누적 거래대금 ≥ $2M · 종가 > $1. 봉은 **토스 5분봉(원시가) 전량**이다 — WS 틱으로 만들면 거래량 눈금이 학습과 달라진다. 스위치 `MODEL_MODE`(false면 추세로 한 줄 롤백). → `docs/domain/모델/` |
| **확률** (모델) | 모델이 낸 상단 장벽(+5%) 선터치 확률(0~1). **임계값과 비교해서만** 쓴다 — 확률로 수량·비중을 정하지 않는다. |
| **임계값** (모델) | 학습 구간 예측 확률의 상위 1% 분위(`core/model/model.json`의 `threshold`). 테스트 구간 분포를 참조하면 미래 참조라 금지. |
| **모델 청산선** | 진입 평단 기준 세 선 — 익절 +5%(`TAKE_PROFIT`) · 손절 −2%(`STOP_LOSS`) · 시간 120분(`TIMEOUT`). 먼저 닿는 것으로 **전량**, 봉 마감을 기다리지 않는다. 동시면 하단 먼저(백테스트 보수 규약). 물타기 없음. 값의 정본은 `MODEL_CONFIG`. → `core/model/exitRule.ts` |
| **Feature 33개** | 모델 입력 — 등락률·봉 모양·거래량 비율·이동평균·RSI·MACD·스토캐스틱·볼린저·ATR·당일 시가 대비·전일 종가 대비. 계산식 정본은 `financial-analyze/docs/FEATURES.md`이고 `core/model/features.ts`가 그 1:1 이식본이다. **결측은 null 그대로 넘긴다**(0으로 채우면 안 된다 — 모델이 결측 가지를 따로 배웠다). |
| **추세** (보존·롤백) | 5분봉 종가 SMA 4선(분봉5선·20선·60선·120선)의 묶음(일선=일봉 평균, 별개). 봉 부족 시 null(≠0). **순수 상태기계**(2026-08-21): 4선이 모두 상승으로 **바뀌는 봉**에 진입(플립 엣지 — 상승이 이어지는 동안엔 재발화 없음), **하나라도 안 오르면** 무조건 청산 — 청산은 **봉 마감을 기다리지 않는다**(2026-08-22): 진행 중(미완성) 봉을 마지막 봉으로 덧붙여 1초마다 다시 재고(`evaluateTrendLive`, 진행 봉당 1회 발화) 차트에 그려진 4선과 같은 순간에 나간다. 진입 BUY는 반대로 **봉 마감 확정에서만**(봉 중간 가짜 플립 진입 금지). 롤백은 `TREND_LIVE_SELL=false`. 진입 필터는 발주 직전 추격 포기(매도1호가 > 신호가 +1%, `core/trend/entryGate.ts`)와 틱속도 하한만 남았다 — 2봉 확인·종가>ma60·챱 차단·감시 요건·손절선은 전부 뺐다(폐기는 이벤트로 남김). 봉 주기·손절 % 값은 `core/trend/bars.ts`·`TREND_CONFIG`가 정본. → `docs/domain/추세/` |
| **플립** | 직전 봉에는 4선이 모두 상승은 아니었는데 이번 봉에 모두 상승이 된 그 봉. 추세의 유일한 진입 시점. |
| **분봉N선** | 최근 N개 봉 종가의 단순 평균(N=5/20/60/120, 봉=추세의 5분봉). 추세를 구성하는 선 하나. |
| **변곡점** (보존·롤백) | 평활된 가격 곡선의 기울기 부호가 바뀌는 지점. 상승 변곡점(−→+, 바닥)=진입, 고점 변곡점(+→−, 천장)=청산. 2026-08-18 추세로 교체, `TREND_MODE=false`로 복귀. → `docs/domain/변곡점/` |
| **기울기** (단독) | SG 감지기 내부의 평활 1차 미분(%/청크). 변곡점 판정 전용. 표시용 기울기/10초는 별개 용어. |
| **청크** | 리샘플 단위(3초 평균 1개, 고정). 변곡점·사다리 판정 주기. |
| **사다리** (보존·롤백) | 변곡점 모드의 진입 감지기. 간격 g%만큼 실제 하락할 때마다 홀 1회, 홀 N번 누적 시 바닥으로 인정해 BUY. 진입 전용. → `docs/domain/사다리/` |
| **홀** | 사다리의 가상 매수 카운트 1회 (= 누적 낙폭 g% 진행). |
| **BUY 게이트** | 거래량 스파이크·체결강도 AND 필터. 입력 null이면 통과(fail-open). 속도 게이트(minTickRate)는 별개. |

## 포지션 (얼마·조건)

| 용어 | 정의 |
|---|---|
| **그리드** | 포지션(수량 N·평단 P)이 생긴 뒤 물타기(P×(1−매수폭))·익절(P×(1+매도폭))을 정하는 규칙. 신호도 체결도 그리드 몫이 아니다. → `docs/domain/그리드/` |
| **매도그리드** | 그리드의 OCO 지정가 구현 — 두 지정가를 항상 걸어 둔다. 상태 IDLE/ARMED/SOLD/FAULT. |
| **조건부 그리드** (현행) | 그리드의 신호 연동 구현 — 지정가 없이 신호가 왔을 때만 매수선/매도선 문턱을 판정해 매매 지시(side·qty)를 낸다. |
| **매수폭/매도폭** | 비대칭 분리 설정(기본 5%/2%). 매수폭은 넓게(올인 지연), 매도폭은 좁게(반등 요구 축소). |
| **물타기** | 평단 아래에서 추가 매수. 수량은 **최초 진입 수량 고정**(ADR 0001), 횟수 상한 없음. |
| **앵커** | 선을 계산하는 기준가. 사다리=트레일링 고점, 매도그리드 매수 다리=min(평단, 현재가)(ADR 0002), 그 외=평단. |
| **재브래킷** | 물타기 체결 후 잔고 재조회 → 새 평단으로 두 선 재설정. |

## 실행 (체결까지)

| 용어 | 정의 |
|---|---|
| **매매** | 방향·수량을 받아 현재가 지정가 → 미체결이면 **정정**으로 추격 → 체결(DONE) 또는 취소선(CANCELLED). 판단하지 않는다. → `docs/domain/매매/` |
| **취소선** | 매매가 추격을 포기하는 조건. 문턱값은 판단(그리드/추세) 소유, 매매엔 술어로 주입. |
| **리프라이스** | 매도 지정가를 매수1호가로 정정해 따라가는 정책. 시간 양보 없음(ADR 0003). |

## 조율

| 용어 | 정의 |
|---|---|
| **오토파일럿** | 단타 중앙 관리자. 상태 IDLE/SCANNING/ENTERING/HOLDING/EXITING/PAUSED/FAULT(활성 종목 사이클에서 파생). 모드 스위치·동시 종목·FAULT 격리·청산 사유. → `docs/domain/오토파일럿/` |
| **포지션 관리자** | 종목 1개의 진입 후 관리 인터페이스(인계·신호·틱·폴·해제 → 결과값 `holding`/`sold`/`isolated`)와 그 어댑터들 — 모델 청산(+서킷)·추세 청산(+서킷)·변곡점 조건부 그리드(규칙+매매형), OCO 매도그리드(롤백 보존). 어느 모드가 켜지는지는 `resolvePositionMode` 한 곳(**모델 > 추세 > 변곡점 > OCO**). 청산 사유는 매도를 시작한 자리에서 정한다. → `features/scalper/positionManager.ts` |
| **사이클(Run)** | 종목 1개의 매수→청산→종료 1회. WATCH_BUY/BUYING/HOLDING/SELLING/DONE/FAULT. |
| **청산 사유** | TAKE_PROFIT(모델 익절선 +5%) · STOP_LOSS(모델 손절선 −2%, 추세 모드에서는 추세 손절선) · TIMEOUT(모델 시간 청산 120분) · SELL_SIGNAL(신호 — 추세·변곡점 경로) · STOP(사용자) · CIRCUIT(서킷) · MANUAL(앱 밖 매도를 잔고로 인지) · USER_SELL(앱 안에서 게이지 두 번 눌러 요청한 전량 매도, 2026-08-22). 정본은 `core/cycle`의 `ExitReason`. |
| **트레이딩 리스트** | 신호 감지기를 상시 부착하는 최대 30종목. 순위 원천에서 채운다. |
| **감시(watched)** | 트레이딩 리스트 중 틱/초 상위 3개. **예열·UI 우선순위 전용** — 판정은 리스트 전 종목에 대해 돈다(모델은 스캐너가, 추세는 상시 부착 감지기가). 진입 자격은 minTickRate가 정한다. |
| **순위** | 트레이딩 리스트를 채우는 원천(토스 8종·한투 7종) 선택 규칙. 원천별 켬·개수·(한투) 기간창, 카탈로그 순서=우선권, 켜진 개수 합 ≤ 30(ADR 0005). → `docs/domain/순위/` |
| **위험포함/위험미포함** | 토스 순위의 관리종목(투자위험·경고 계열) 포함 여부. 미포함 = `KRX_MANAGEMENT_STOCK` 필터로 제외. |

## 예외·지표

| 용어 | 정의 |
|---|---|
| **서킷** | LULD 정지 종목 상태. 상킷=홀드, 하킷 2연속부터 정지 중 지정가 매도(ADR 0004). 관측 단계(`CIRCUIT_MODE=false`). → `docs/domain/서킷/` |
| **켈리** | 실거래 승률·손익비로 계산한 포지션 비중 제안. 참고 지표. → `docs/domain/켈리/` |
| **틱/초** (속도) | 최근 10초 윈도우 안 체결 틱 수 ÷ 10. 항상 ≥ 0. 표기는 틱/10초(×10, 정수). 진입 자격(minTickRate)에 쓰인다. |
| **기울기/10초** | 직전 10초 봉 평균가 대비 현재 10초 봉 평균가의 %변화. 두 봉 중 하나라도 비면 null(≠0). 표시 전용. → `docs/domain/기울기/` |
