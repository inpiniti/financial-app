# CONTEXT — 도메인 용어집

이 앱(변곡점 단타 앱)의 정본 용어. 상세는 `docs/domain/` 각 문서 참조.

| 용어 | 정의 |
|---|---|
| **변곡점** | 평활된 가격 곡선의 기울기 부호가 바뀌는 지점. 상승 변곡점(−→+, 바닥)=진입, 고점 변곡점(+→−, 천장)=청산. → `docs/domain/변곡점/` |
| **틱/초** (속도) | 최근 10초 윈도우 안 체결 틱 수 ÷ 10. 종목이 지금 얼마나 활발한가(빈도). 항상 ≥ 0. 표기는 틱/10초(×10, 정수). |
| **기울기/10초** | 직전 10초 봉 평균가 대비 현재 10초 봉 평균가의 %변화. 부호가 방향(−하락/+상승). 두 봉 중 하나라도 비면 null이며 0(평균 동일)과 다르다. → `docs/domain/기울기/` |
| **기울기** (단독) | SG 감지기 내부의 평활 1차 미분(%/청크). 변곡점 판정 전용 — 기울기/초와 혼용 금지. |
| **청크** | 리샘플 단위(기본 3초 평균 1개). 변곡점 판정 주기. |
| **사다리** | 진입 감지기(현행). 간격 g%만큼 실제 하락할 때마다 가상 매수(홀) 1회, 홀 N번 누적 시 바닥으로 인정해 BUY. |
| **홀** | 사다리의 가상 매수 카운트 1회 (= 누적 낙폭 g% 진행). |
| **매도그리드** | 체결 후 청산·물타기를 맡는 평단±폭 OCO 상태기계. 매수폭·매도폭은 비대칭(분리 설정). |
| **앵커** | 그리드/사다리의 기준가. 사다리는 감시 시작가(트레일링 고점), 그리드 매수 다리는 min(평단, 현재가). |
| **BUY 게이트** | 거래량 스파이크·체결강도 AND 필터. 입력 null이면 통과(fail-open). |
| **추세** | 분봉 종가 SMA 4선(분봉5선·20선·60선·120선)의 묶음. "일선"이라 부르지 않는다(일선=일봉 평균). 봉 부족 시 null(≠0). → `docs/domain/추세/` |
| **분봉N선** | 최근 N개 분봉 종가의 단순 평균(N=5/20/60/120). 추세를 구성하는 선 하나. |
