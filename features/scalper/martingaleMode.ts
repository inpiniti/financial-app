// 배수 물타기 시험 모드(2026-08-27, ADR 0006) 단일 스위치.
//
// 세 층이 **같은 상수 하나**를 읽는다 —
//   · autopilotManager: 슬롯을 물타기 모드(1분봉)로 만들고 추세 워밍업(분봉 시드)을 1분봉으로 돌린다. 모델 스캐너는 돌지 않는다.
//   · feedSlot: 1분봉 합성 + 4선으로 진입(정배열·5선 돌파)·물타기(5선 변곡) 신호를 낸다.
//   · positionManager: 포지션 규칙을 MartingaleRule(익절 사다리 +3/+2/+1, 배수 물타기, 마감 청산)로 고른다.
// 실제 활성화는 이 상수 AND 매니저의 `martingale` 주입. 모델·추세보다 **우선**한다(시험 모드라 켜면 이것만 돈다).
// false로 두면 모델(MODEL_MODE)로 **한 줄 롤백**된다.
//
// 근거·경고: financial-analyze docs/analysis/2026-08-27_물타기-변형.md §15 + 2026-08-27_앱-적용-계획.md §3.
//   백테스트(슬리피지 0·고가 터치 체결)에서 승률 72%·자본대비 +1.5%였으나 실측 슬리피지(0.8~1.9%p)에서는
//   음수가 유력하다 — 소액 실거래로 판정하는 것이 이 모드의 목적이다.
export const MARTINGALE_MODE = true;

/** 물타기 모드 봉 주기(분) — 백테스트 규약(1분봉). */
export const MARTINGALE_BAR_MINUTES = 1;
