# 분석 → 수정 개선 일지

> 운영 방식(2026-08-19 확정): 사용자가 실거래 데이터(`trade_results` CSV 등)를 넘긴다 → 토스 분봉 등으로 재현·비교 분석 → 근거가 선 것만 코드로 수정 → **분석 문서 1건 + 이 일지에 1줄씩** 남긴다.
> 받은 원본 데이터는 `분석용 데이터/YYYY-MM-DD.csv`(받은 날짜)로 함께 커밋한다. 분석 문서는 `YYYY-MM-DD_주제.md`, 이 일지는 날짜 역순으로 "받은 데이터 · 분석 결과 · 바꾼 것(커밋) · 안 바꾼 것과 이유 · 다음에 볼 것"을 적는다.
> 절대 수치보다 **변형 간 상대 비교**로 판단하고, 하루치로 뒤집지 않는다. 판단이 뒤집힌 항목은 지우지 않고 취소선으로 남긴다.
> 날짜 관례: **거래일 = 미국 거래일(ET 날짜)**, 문서 파일명·일지 날짜 = 작성일(KST). CSV `entry_at`은 UTC — 세션 구분(프리 04:00~09:30 / 정규 09:30~16:00 / 애프터 16:00~20:00 / 오버나이트 20:00~04:00 ET)은 ET로 환산해 판단한다. 토스 분봉 `dt`는 ET 오프셋 포함.

---

## 2026-09-01 — 마틴게일 닷새 55건: 물타기가 수익의 80%였지만, 사용자 결정으로 물타기 제거 + ±3% 손절/익절 + 주간거래 진입 제외

- **받은 데이터**: `trade_results` 157건 중 martingale 55건(08-27~31, +$16.73) — 원본 [분석용 데이터/2026-09-01.csv](분석용%20데이터/2026-09-01.csv). 사용자 질문: "물타기 없이 가는 게 금전적 이득 아닌가 확인해달라" + 요청(주간거래 제외·물타기 제거·−3% 손절).
- **분석**: [2026-09-01_물타기-vs-손절-비교.md](2026-09-01_물타기-vs-손절-비교.md) — **가설 반증**: 물타기 발생 12건 전부 회복해 이익 종결(+$13.37), "물타기 없음+−3% 손절" 가정이면 전원 손절 −$0.54(전체 +$16.73 → +$2.82). 손절 −4%/−5% 변형도 동일(11건 중 9건이 최소 −7.5~−26%까지 낙폭 — 어디 둬도 잘리고 손실만 커짐). 다만 물타기 노출이 AIM $1,037(계좌 ~15%)까지 불었던 미관측 꼬리 확인. 주간거래 진입은 2건 +$0.15로 무의미.
- **바꾼 것**(사용자 결정 — 데이터 반대에도 "직접 눈으로 보겠다", [ADR 0007](../adr/0007-물타기-제거-3퍼센트-손절.md)): `core/martingale` 물타기(5선 변곡 add 신호·배수 매수·익절 사다리) 제거, 손절 −3%(STOP_LOSS, 취소선 없음) 도입, 익절 +3% 고정, 진입 세션 프리·정규·애프터만(`isMartingaleEntryBar`). 진입 규칙·매도1호가 추종·19:55 마감 청산·strategy 이름 `martingale`은 유지.
- **안 바꾼 것**: 물타기+종목당 투입 상한 절충안(권고로만 — 재검토 시 ADR 0007부터), 모델·추세 모드.
- **다음에 볼 것**: ① STOP_LOSS 건수/일 ② 손절 슬리피지(exit_snapshot.line 대비) ③ 손절 뒤 이벤트 재진입 성과 ④ +3% 도달률 ⑤ 며칠 뒤 절충안 재검토.

## 2026-08-27 — 08-26 모델 2건: 지연 수정 검증 완료, 청산 슬리피지 첫 실측 −0.93/−0.77%p(손익분기 0.6%p 초과) → 문제는 신호가 아니라 비용 층

- **받은 데이터**: `trade_results` 102건 중 08-26 model 2건(MSS·XPON, 둘 다 STOP_LOSS, −$0.29) — 원본 [분석용 데이터/2026-08-27.csv](분석용%20데이터/2026-08-27.csv). 사용자 질문: "어제도 잃었다, 방향이 틀린 것 같다".
- **분석**: [2026-08-27_청산-슬리피지-첫-실측과-방향.md](2026-08-27_청산-슬리피지-첫-실측과-방향.md) — ① 봉 라벨 수정 작동 확인(진입 봉 마감 +12·+18초, 보유 65·17분, 08-25의 13~29초 즉시 손절 소멸) ② **청산 슬리피지 첫 실측 −0.93%p·−0.77%p** — 청산 연구 2의 손익분기 0.6%p를 둘 다 초과, 왕복 비용이 손절폭 2%의 절반~전부 ③ 6일 102건 가격대별: **$10 미만 93건 전 구간 음수(−$8.32), $10 이상 9건만 양수(+$3.04)**, PF 0.75 — 전략이 추세→모델로 바뀌어도 같은 구조. 결론: 신호가 틀린 게 아니라 **신호 마진 < 체결 비용**(틱 $0.01이 가격의 0.3~1%인 풀에서 −2% 손절은 잡음 크기). 6일의 개선은 전부 신호 층, 비용 층은 손댄 적 없음.
- **바꾼 것**: 없음(보고서만).
- **권고(사용자 판단)**: ~~① 종목 풀 가격 하한 $5~10~~(같은 날 철회 — 청산 연구 08-24 가격대별 PF는 $1~20이 $100+보다 우위, 상한 $10 미만은 사용자의 테스트 부담 기준. 진입 슬립도 가격대와 무관(0.4~1.0%)이라 "틱 크기" 논리 철회) → **비용의 주범은 체결 방식(현재가 지정가→정정 추격)**. 남는 권고: ② 청산을 선 가격 지정가 → N초 후 추격으로(financial-analyze로 선 지정가 체결률 먼저) ③ 손절폭 확대 대신 문턱 상향으로 건수 감소.
- **다음에 볼 것**: 청산 슬립 10건 이상 가격대별 · 하한 적용 시 후보 수 · MSS 기록의 kind=STOP_LOSS인데 line=트레일 값인 라벨 정의.

## 2026-08-26 — 모델 첫 실거래 6건: 토스 봉 라벨(끝 시각) 오독 → 판정 상시 5분 지연

- **받은 데이터**: `trade_results` 중 model 6건(08-25 정규장, 승률 1/6 · −$0.57) + 스크린샷 4장 — 원본 [분석용 데이터/2026-08-26.csv](분석용%20데이터/2026-08-26.csv). 사용자 보고: "매수 신호인데 안 산다 / 점선 봉이 2개 / −2~4%에 팔면 그 뒤 급등".
- **분석**: [2026-08-26_모델-첫날-봉-라벨-오독.md](2026-08-26_모델-첫날-봉-라벨-오독.md) — **토스 c-chart의 dt는 봉의 끝 시각**(실측: 진행 중 봉 dt가 미래, min:5 main 라벨 09:35~16:00). 앱은 시작으로 해석 → ① 컷오프가 방금 닫힌 봉을 미완성으로 버려 **판정이 상시 한 봉(5분) 늦음**(급등 꼭대기 진입 → 즉시 손절 → 그 뒤 급등의 구조 원인) ② 점선 봉 2개 ③ 세션 경계 오판(15:55 봉 버림·09:25 프리장 봉 오인).
- **바꾼 것**: `parseTossMinuteCandles` minuteKey = dt − 주기(봉 시작 정규화, 전 경로), `isMainSessionBar` = 끝 라벨(시작+주기) ∈ [09:31,16:00](학습 필터 정본과 일치), 픽스처 실측 규약으로 재작성. 테스트 889건 통과.
- **안 바꾼 것**: 청산 규칙(−4%대 청산은 슬리피지 — 지연 수정 후 재평가), 재진입 규칙(봉마다 재판정 구조로 충분).
- **다음에 볼 것**: ① 수정 후 진입 시각이 봉 마감 +5~10초인지 ② 순수 슬리피지(신호 봉 종가 vs 진입가) ③ SWVL류 미체결 신호 사유(기록 이벤트) ④ 15:55 봉 신호 실존 여부.
- **같은 날 백테스트(financial-analyze [2026-08-26_청산-연구-2](../../../financial-analyze/docs/analysis/2026-08-26_청산-연구-2.md))**: ⚠ 채택안(트레일 −5%/손절 −2%)은 **청산 선 체결이 1%p 불리하면 PF 0.83으로 손실**(손익분기 슬리피지 ≈ 0.6%p, 08-25 실측 평균 0.76%p — 단 지연 버그 날). 사용자 제안 "확률<X% 매도"는 순수형이 승률 59%지만 꼬리 −84%, 넓은 손절과 조합(확률<20% + 트레일 −8%/−4%)이 +53.3(1.40배)로 최선이나 2026년 구간에서 채택안에 짐 → **채택안 유지(보류)**. 다음 관문은 슬리피지 실측 — **계측 추가함**: 정산 기록 `exit_snapshot`에 매도 결정 순간의 `price`(판정 틱가)·`line`(매도선)·`peak`(고점)·`kind`·`ts`가 실린다(테이블 변경 없음, jsonb). 계산식: **청산 슬리피지 = (exit_price − line) / line**, 청산 지연 = exit_at − ts, 진입 슬리피지 = (entry_price − entry_snapshot.price) / entry_snapshot.price.

## 2026-08-22 — 차트와 감지가 어긋나 청산이 늦던 문제(코드 대조, 재현 없음)

- **받은 데이터**: 사용자 보고("그래프와 감지가 일치하지 않는다 / 너무 늦게 판다 / 150%→70%→20%에 팔았다 / 몇 개는 안 팔아서 직접 매도했다"). 08-22 종목·수익률 JXG −19 · NXTT +12 · ADXN +28 · NCTY −15 · HOWL 0 · KNRX 0. **수동 매도라 `trade_results`에 08-22 행이 0건**([분석용 데이터/2026-08-22.csv](분석용%20데이터/2026-08-22.csv)) — 재현 시뮬 불가, 코드 대조로만 판정.
- **분석**: [2026-08-22_차트-감지-불일치와-청산-지연.md](2026-08-22_차트-감지-불일치와-청산-지연.md) — 어긋남은 두 겹. ① **봉 주기가 달랐다**: 차트 기본 1분봉 vs 엔진 5분봉 → 화면의 ma5(5분)와 엔진의 ma5(25분)는 다른 선이다. ② **엔진이 진행 중 봉을 안 봤다**: `FeedSlot`이 봉 마감(non-null 반환) 때만 판정 → 눈에 보이는 꺾임을 최대 5분 늦게 안다.
- **바꾼 것**: ① `evaluateTrendLive`(core/trend/signal) — 진행 중 봉 포함 재판정, **SELL만** 발화(BUY는 봉 마감 확정 유지, 가짜 플립 진입 방지). `FeedSlot`이 1초 스로틀·진행 봉당 1회로 낸다. 롤백은 `TREND_LIVE_SELL = false` 한 줄 ② 차트 분봉 기본값을 `TREND_BAR_MINUTES`로, 진행 중 봉은 속 빈 캔들+점선 경계 ③ 차트 아래 **엔진 판정 줄**(봉 마감 기준 / 진행 중 봉 포함 기준 / 봉 수 · ma120 미완성이면 몇 봉 더 필요한지) ④ **늦은 합류 철회**(`bd2d85e`·`9aa1374` revert — 앱 켠 직후 전 종목 동시 시드가 곧 "달리던 종목 일괄 매수"였다) ⑤ 챗봇 도구 4종(`getMinuteCandles`·`getPeriodChart`·`getEvents`·`getSettings`)과 대화 기록 저장.
- **안 바꾼 것**: 봉 주기 5분(1분봉은 슬리피지에 구조적 열세 — 08-21 §6), 손절선 없음(진행 중 봉 판정이 그 구멍을 상당 부분 메운다), 진입 규칙(이번 문제는 청산 타이밍).
- **다음에 볼 것**: ① **"5분을 훨씬 넘겨 안 팔린 건"이 실제로 있었나** — 있었다면 원인은 토스 시드가 121봉을 못 채워 `ma120`이 null(fail-closed로 매수·매도 둘 다 막힘)이거나 WS 틱 끊김이다. 이제 차트 아래 판정 줄에 그대로 보인다 ② 진행 중 봉 SELL이 가짜 꺾임에 얼마나 걸리는지(승자를 자르면 `TREND_LIVE_SELL=false`) ③ 늦은 합류를 뺀 뒤 진입 기회 감소폭.

## 2026-08-21 — 순수 상태기계 코드 반영: 5분봉 · 플립 진입 · 4선 깨짐 청산 · 필터/손절 제거

- **받은 데이터**: 사용자 확정("5분봉으로, 그동안 만든 추가 규칙 없이 순수한 상태기계로") — 근거는 [2026-08-21_4선-상태기계-검증.md](2026-08-21_4선-상태기계-검증.md) §5~§6.
- **바꾼 것**: ① 봉 주기 3분 → **5분**(`TREND_BAR_MINUTES`, 시드 토스 min:5도 자동 추종) ② 진입 = **플립 엣지**(allUp(t) ∧ ¬allUp(t−1)) — 2봉 확인·종가>ma60 제거 ③ 청산 = **¬allUp(t)** — 종가<ma5 제거 ④ 챱 차단·감시 요건 제거(`autopilot.handleBuySignal`) ⑤ 손절선 제거(`TREND_CONFIG.stopLossPct` 0.07 → 0). 남긴 것: 추격 게이트(+1%, 실체결 보호 — 시뮬이 평가할 수 없는 층)와 틱속도 하한(설정값, 0이면 꺼짐). 테스트 716건 통과·tsc 통과, CONTEXT·도메인 plan·설정 화면 문구 갱신.
- **안 바꾼 것**: 서킷 도메인(관측 단계 유지), 가격 상한·리스트 원천, 그리드/변곡점 배선.
- **주의(다음에 볼 것)**: ① **BUY가 엣지**라 그 봉에 슬롯 만석·현금 부족·쿨다운이면 기회가 다음 플립까지 사라진다 — 놓친 신호가 이벤트로 얼마나 잡히는지 ② 손절 없는 첫날 — 봉 마감 전 낙폭(재현 최악 −39%)이 실제로 얼마나 나오는지 ③ 5분봉이라 거래 수가 3분 대비 ~60%로 준다(진입 기회 체감) ④ 사흘 in-sample 반영이므로 869일 백테스트로 사후 검증(financial-analyze).

## 2026-08-21 — 4선 상태기계(사용자 제안): 이틀 연속 현행 우위, 백테스트 1번 질문으로 승격

- **받은 데이터**: 사용자 제안("상상상상 되는 순간 사고, 하나라도 꺾이는 순간 파는 것만 반복하면 무릎-어깨 아닌가") — 08-19 28종 풀데이 + 08-20 13종 아침 봉으로 검증.
- **분석**: [2026-08-21_4선-상태기계-검증.md](2026-08-21_4선-상태기계-검증.md) — 슬리피지 포함 08-19 +$8.38→**+$67.98**(챱차단 누적 시 +$73.84), 08-20 −$1.35→**+$0.74**. 분해: 진입(플립 즉시, 지연 4봉→2봉)과 청산(4선 깨짐 — 눌림 한 봉에 안 팔아 MRNA $97→$116, 챱 썰림 절반) 각각 기여, 합이 최고. 대칭 구조라 확인 2봉의 보험 역할을 청산이 대신함. 앞서 기각된 ±3% 익절식과 달리 큰 승자를 안 자름.
- **추가(§5, 순수 버전 3일)**: 조건 싹 뺀 순수(손절·ma60 없음)를 08-18 포함 3일에 — 매일 벌진 않음(18일 −$1.45·20일 −$2.25·19일 +$66.15). 3일 합 순수 **+$62.45** vs 현행 +$1.12. 복권 구조(추세 온 날만 큼)는 불변, 가치는 손익분기 날씨 하락. 손절 −7%는 날마다 갈림(18일 −$11.17 독 / 20일 도움) — 꼬리 보험으로 유지하되 백테스트 판정.
- **추가(§6, 봉 주기)**: 토스 원본 1·3·5분봉 3일 비교 — 1분 참패(−$91, 1,080건 슬리피지 세금), 3분 +$67, **5분+손절 +$97.34로 최선이고 유일하게 3일 전부 비음수**(+$0.9/+$94.2/+$2.2). 봉 원천 민감성 발견: 토스 봉 vs 1분 자체 집계 봉이 미세하게 달라 신호가 어긋남 — 백테스터·앱은 같은 봉 원천 필수.
- **바꾼 것**: 없음(보고서만) — 어제 결정(설정 동결·백테스트로 판단) 유지. 사흘 in-sample.
- **다음에 볼 것**: ① financial-analyze 869일 백테스트 1번 질문으로(스펙 문서에 상태기계 변형 포함: 순수/+손절/현행) ② 재진입 의미론 결정(전환 순간만 vs 비보유∧상상상상) ③ 통과 시 `TREND_STATE_MACHINE` 플래그 구현.

## 2026-08-21 — 셋째날(08-20) 25건: 개선 3종 전부 작동 확인, 손실 원인은 풀의 날씨

- **받은 데이터**: `trade_results` 25건(08-20 ET 04:06~10:18, ~11시 수동 중단) — 원본 [분석용 데이터/2026-08-21.csv](분석용%20데이터/2026-08-21.csv). 사용자 질문: "나아지는 게 안 보인다, 불가능한 걸 시키나".
- **분석**: [2026-08-21_셋째날-25건-개선검증.md](2026-08-21_셋째날-25건-개선검증.md) — 승률 8%, −$4.29. 반영 개선 3종 전부 작동(챱 진입 0/25, 추격 +1% 초과 1건뿐(JZ, 게이트 스킵 추정), RITR 저점 진입 +43.5%). 청산 변형 재생(손절없음/−12%/종가판정) 전부 수익률 악화 → −7% 유지. 손실 원인 = 풀에 진짜 추세가 없던 날(전 진입 밴드폭 9~134% 펌프) + 유일 승자 RITR이 $0.12×1주(+$0.05). $10 균일 사이징도 3일 모두 악화 — 사이징 문제 아님.
- **바꾼 것**: 없음(보고서만) — 사흘 연속 규칙 변경 금지, 다음 판단은 백테스트로.
- **다음에 볼 것**: ① BUY 폐기 이벤트 첫 데이터 ② 게이트 스킵 로깅 ③ **financial-analyze 백테스터에 현행 규칙 이식(869일×top30 기대값)** ④ 설정 동결하고 데이터 축적.

## 2026-08-20 — 분석 4건 코드 반영: 챱 차단·감시 요건·폐기 로깅·가격 상한 분리

- **받은 데이터**: 사용자 확정(분석 내용대로 수정 + "그리드로 넘길 땐 틱속도 빠른 것만") — plan [docs/development/2026-08-20_추세-진입-개선-plan.md](../development/2026-08-20_추세-진입-개선-plan.md).
- **바꾼 것**: ① 챱 차단 — 신호봉 밴드폭(3분 20봉) < 1.5%면 BUY 무시(`TREND_MIN_BAND_WIDTH_PCT`) ② 감시 요건 — BUY는 감시(틱속도 상위 3종)에 든 종목만(절대 문턱 대신 상대 선택) ③ BUY 폐기 사유 이벤트(종목당 10분 스로틀 — 무음 폐기 금지) ④ 리스트 가격 상한 분리 — 수량 모드에서 새 설정 `maxPriceUsd`(기본 $200), 금액 모드는 기존(진입금액=상한). 테스트 719건 통과.
- **안 바꾼 것**: 청산 일체(종가<ma5·손절 −7%), 봉 3분, 과열 회피 필터(금지), 트레일링·본전스탑·1분봉(기각 확정). 속도 설정값(20→1~3)은 앱 설정에서 사용자가 직접.
- **다음에 볼 것**: ① BUY 무시 이벤트 분포(챱/감시/속도)와 막힌 신호의 이후 궤적 ② $20 초과 종목 진입 성적 ③ 챱 문턱 1.5% 재검증 ④ 진입 슬리피지 분포 변화.

## 2026-08-20 — 지표로 승패를 미리 가릴 수 있나 (보고서만)

- **받은 데이터**: 사용자 질문(이익 종목만 골라 들어갈 수 없나 — RSI·MACD·볼린저·스토캐스틱·VWAP·피봇·거래량으로) — 풀데이 시뮬 302건 진입의 사전 지표 12종 계산.
- **분석**: [2026-08-20_지표-승패-구별-검증.md](2026-08-20_지표-승패-구별-검증.md) — 단일 지표 분리력은 전부 미미(0.3~0.5%p). 패턴은 "중간 구간이 최고"지만 중간만 골라내면 큰 승자(MRNA 밴드폭 12.8%·YJ 47.9% = 과열 구간 거주)를 전멸시켜 달러 +$2.45로 오히려 열위. **유일한 공짜 필터 = 챱 차단(볼린저 밴드폭 <2% 신호 무시): 95건 −75% 제거, 큰 승리 9/9 보존, 달러 +$8.4→+$69.8.** 펌프 vs 진짜 추세는 진입 시점 지표로 구별 불가(사후에만 보임) — 그 구별 불가능성의 보험료가 손절 −7%. 과열 회피 필터는 채택 금지(수익원 제거).
- **바꾼 것**: 없음(보고서만). 챱 차단은 후보로 등재(문턱 2%는 in-sample — 적용 시 1.5%로 보수적으로, 이벤트 로그로 검증).
- **추가 검증(§6, 사용자 제안)**: 봉간 변동율(이전 봉 대비 가격·거래량)을 매수·매도에 쓰는 안 — **양쪽 다 기각**. 진입: 챱 차단 위에 dP1>0/dV1>1/동반 요구를 얹으면 달러 제자리·큰 승리 4~6/9로 훼손(달러 승자 = MRNA 눌림 재진입·SNDK가 전부 음봉 진입). 청산: 봉 등락률 −3%/−5%·음봉+거래량 2배 조기 청산 전부 개선 없음(−7% 틱 손절이 극단을 이미 담당, 얕은 봉간 하락 = 정상 눌림). 거래량 급증=클라이맥스 경고만 재확인(차단 필터로는 금지).
- **다음에 볼 것**: ① 챱 차단 문턱의 타일 검증(다음 거래일 데이터) ② 채택 시 속도 필터와의 역할 정리(속도=유동성 하한, 밴드폭=변동성 하한).

## 2026-08-20 — 풀데이 시뮬(28종): 중단은 옳았다, 문제 순서는 설정>종목무차별>단타

- **받은 데이터**: 사용자 질문(중단 안 했으면? 단타·전략·설정 중 뭐가 문제?) — 같은 28종 분봉, ET 04:00~19:00(프리+정규+애프터) 이상형 시뮬(속도 필터 없음).
- **분석**: [2026-08-20_풀데이-시뮬레이션-28종.md](2026-08-20_풀데이-시뮬레이션-28종.md) — 중단 이후(09:55~19:00) 시뮬도 −$20.5로 **중단은 옳은 판단**. 같은 아침 창에서 이상형 봇 +$28.8 vs 실제 −$6.1 — 간극 = 가격 상한(MRNA 배제) + 속도 필터(저점 신호 폐기) + 추격. MRNA 하나가 +$97, 제외 시 −$89. $20 경계로 손익 갈림(미만 −$21 / 이상 +$29.5). RIVN 21전 0승(무추세 챱) — 전략의 약점은 종목 무차별. **본전 스탑은 풀데이에서 악화(−167→−189%) → 채택 보류로 강등**(방법 간 뒤집힘 — 사용자 아이디어와 같은 기준 적용).
- **바꾼 것**: 없음(보고서만).
- **다음에 볼 것**: ① 속도 2틱/초로 하루 실운영 검증 ② 가격 상한 분리(승인 대기) ③ 오후·애프터 상시 가동 근거 없음 — 세션별 데이터 계속 축적 ④ 무추세 챱 필터(종목 단위) 아이디어 검토.

## 2026-08-20 — 급등주 "저점에 타기" 검토 (보고서만, 코드 수정 없음)

- **받은 데이터**: 사용자 관찰(수익 +20% 갔다가 −10%에 판다 / 최고점 근처에서만 진입한다 / 어제도 급등주 많았다 / 11시 넘어 수동 중단, 이후 거래 없음) + 같은 28종 분봉.
- **분석**: [2026-08-20_급등주-저점진입-검토.md](2026-08-20_급등주-저점진입-검토.md) — 관찰 셋 다 수치로 확인(MFE≥10% 8건 중 4건 손실 종결, 진입 위치 중앙값 ~70%·꼭대기 진입 7건). 트레일링(−5%가 최선이나 −58%)·확장도 필터(승자까지 차단)·눌림목 기계화(건당 +0.14%, 비용 못 이김) 전부 이 종목군에선 답 아님. **결정적: MRNA(+181%, 거래대금 순위)에 현행 규칙 그대로 +86.4%/17건 — 전략이 아니라 "진입금액=가격 상한" 필터가 만든 초저가 펌프 풀이 문제.**
- **바꾼 것**: 없음(보고서만).
- **추가 규명(§5)**: "4선 상승인데 매수 안 하다 꼭대기에서만 산다" = 속도(minTickRate) 10→20이 타이밍 필터가 된 것 — BUY 신호를 속도 미달로 조용히 버리고(autopilot.ts:920, 이벤트 없음) 수직 클라이맥스(거래량 92백분위)에서만 통과. ZNB 04:09 $2.35 +72% 신호 버림 → 04:15 $3.5 꼭대기 매수 사례. 설정값 확인 완료(진입금액 20→10, 수량 1, 속도 10→20).
- **다음에 볼 것**: ① 속도 20 → 1~3틱/초(설정만으로 가능) ② 가격 상한을 진입금액에서 분리하는 설정안(수량 1주 모드 + 상한 $200~300) ③ 풀 개방 후 트레일링 −5% 재검토.

## 2026-08-20 — 둘째날(08-19) 24건: 손해 확대 → 추격 진입 게이트

- **받은 데이터**: `trade_results` 24건(08-19 ET 04:15~09:51, 3분봉·손절 −7% 적용 첫날) — 원본 [분석용 데이터/2026-08-20.csv](분석용%20데이터/2026-08-20.csv). 사용자 관찰: "3분봉 분석 반영했는데 오히려 손해가 커졌다".
- **분석**: [2026-08-20_추세-둘째날-24건-분석.md](2026-08-20_추세-둘째날-24건-분석.md) — 거래 종목 9개 + 토스 거래량·거래대금 순위 19종, 1·3·5분봉 재현(실측 슬리피지 반영).
  - 실거래: 승률 17%, 합계 −97.1%(−$6.12). 24건 중 20건 STOP_LOSS, 손절 체결이 −7% 선보다 평균 −1.68%p 아래. YJ 2건 빼면 −166%.
  - 설정 탓 아님: 같은 날 옛 설정(1분·−5%) 재현은 −212% vs 새 설정 +28% — 이 날 종목군(급등 후 붕괴)이 첫날보다 나빴다. 재현 +28% vs 실제 −97% 간극 = 재현의 구조적 낙관 편향(체결은 신호 순간의 얇은 ask1).
  - 손절 −7%는 범인 아님: 없애면 −97→−70이지만 보유 중 낙폭 −58%(RDAC) 노출, 넓히면(−10/−12%) 오히려 −171/−206으로 악화 → 유지.
  - **진짜 범인 = 추격 진입**: 체결이 신호가보다 +1% 이상 뛴 진입, 이틀 23건 합계 −89%(승 4). 게이트(+1% 초과 시 포기) 소급 시 이틀 합산 −88% → +1%.
  - 순위 19종(정상 유동성)에선 손절 0회·건당 원수치 +0.9%(3분) — 손절·추격 문제는 저가 급등주에서만 난다. 게이트가 종목 필터를 겸함.
- **바꾼 것**: 추격 진입 게이트 — 발주 직전 매도1호가 > 신호가 ×1.01이면 진입 포기(`core/trend/entryGate.ts` `TREND_ENTRY_CHASE_MAX_PCT`, `autopilot.commitBuy`, 신선한 호가 있을 때만 판정).
- **안 바꾼 것**: 손절 −7%(보험료, §3), 봉 주기 3분(방법 간 결론 뒤집힘 — 하루 만에 재변경 금지), 세션 필터(정규장 4건도 전패 — 세션이 아니라 종목군 문제), 5분봉·3봉 연속(보류).
- **다음에 볼 것**: ① 게이트 발화 빈도("진입 포기 · 추격 상한" 이벤트)와 막힌 종목의 이후 궤적(놓친 이익 vs 피한 손실) ② 게이트 통과분의 승률·STOP_LOSS 비율이 슬립 ≤0.5% 구간(이틀 33건 +34.8%) 수준으로 오는지 ③ 손절 체결 슬리피지(−1.68%p)가 게이트 이후 줄어드는지 ④ 재현 낙관 편향 보정값 갱신.

## 2026-08-19 — AIXC 서킷(08-18) → 서킷 도메인

- **받은 데이터**: 사용자 경험(AIXC 하킷 2연속 뒤 한투 앱에서 2.2 수동 매도, 앱 기록 없음) + 토스 1분봉 08-18 정규장 전체(정지 24회, 09:35~11:59 집중, 정지 5분/10분, 재개 창 1~2봉·체감 수 초).
- **분석**: 도메인 문서 [`docs/domain/서킷/2026-08-19_서킷-개념과-설계.md`](../domain/서킷/2026-08-19_서킷-개념과-설계.md) §1~§2 — 서킷 종목에서 추세 규칙이 깨지는 이유(봉 안 닫힘·재개는 단일가 경매·정지 중 호가 뒤집힘), 정지 직전가 수열(하킷 1회는 꺾임 아님, 2연속부터 내리막). 규칙별 대입: 첫 정지 매도 +0.4 / 첫 하킷 +0.7 / **하킷 2연속 +1.2(사용자 실매도)** / 안 팔면 +0.3.
- **바꾼 것**: 서킷 도메인 구현(관측 단계, `CIRCUIT_MODE=false`) — 정지·재개·서킷 상태 이벤트, 하킷 2연속·손절선 → 정지 중 지정가(−12%) 매도(스위치 켜면), 앱 밖 수동 매도를 잔고 재확인(2분×2회)으로 `MANUAL` 정산·기록. 계획: [`2026-08-19_서킷-감지-재개매도-plan.md`](../domain/서킷/2026-08-19_서킷-감지-재개매도-plan.md).
- **안 바꾼 것**: 호가 도메인·10호가 구독(정지 중 호가는 노이즈, 1호가는 틱에 있음), 고립 정지 대응(정상 로직 유지 — 08-18 대부분의 서킷 종목이 이 경우), 서킷 종목 진입 차단(표본 더).
- **다음에 볼 것**: 서킷 종목 만나면 이벤트로 ① 정지 중 한투 주문 접수 여부 ② 정지 중 WS 틱(호가만 오는지) ③ 재개 창 실측(60초 미만·체감 5초) ④ 45초/30건 문턱 오탐 → 확인되면 `CIRCUIT_MODE=true`.

## 2026-08-19 — 첫 실거래일(08-18) 42건

- **받은 데이터**: `trade_results` 42건(08-18 KST 17:03~00:27, 1주, 프리·정규 초반) — 원본 [분석용 데이터/2026-08-19.csv](분석용%20데이터/2026-08-19.csv). AIXC 수동 청산 1건(기록 없음).
- **분석**: [2026-08-19_추세-첫날-42건-분석.md](2026-08-19_추세-첫날-42건-분석.md) — 토스 1·3·5분봉으로 규칙 변형 재현.
  - 실거래: 승률 29%, 합계 +9.0%(프리 −36.6% / 정규 +45.6%). 이익은 4건(SLE·IPST·PFSA×2)이 다 냄. 진입 슬리피지 실측 0.65%/건.
  - 1분봉은 거래 수 × 슬리피지에 구조적으로 짐(재현 −98~−153%). 3분봉은 거래 1/3·건당 이익 4배, 슬리피지 반영 후 +82~90%.
  - 청산 "종가<ma5"가 "ma5 기울기"보다 모든 봉 주기에서 우위. 손절 −5%는 꼬리 체결로 손해, −7%부터 비용 소멸.
  - 진입 "상승 2봉 연속"은 유지가 맞음(1봉은 음수, 3봉은 승률↑ 총합 동일). 종가>60선은 사실상 중복(무해).
  - 재진입 앵커·개장 N분 진입 금지는 기각(정상 재진입 차단 / 정규장 개장이 이익 자리).
- **바꾼 것**: `d679be5` 봉 3분(`TREND_BAR_MINUTES`) · 청산 종가<ma5 · 손절 −7% · 시드 토스 `min:3`. `0805cb3`(전날 밤) 손절선 도입. `316420a` 게이지 오늘 최저·최고 + 평단 대비 % + 거래기록 수익률 %.
- **안 바꾼 것**: 세션 필터(프리/정규 결론이 봉 주기에 따라 뒤집힘 → 표본 더), 3봉 연속(총합 동일), 서킷 종목 감지(하루 1종목, 우선순위 낮음), 개장 필터·앵커(기각).
- **다음에 볼 것**: 3분봉 실거래에서 ① 3봉 이내 털림 비율이 줄었나 ② `STOP_LOSS` 건수(−7%가 너무 자주 울리는지) ③ 세션별 승률·평균(표본 30~50건 전 켈리 판단 금지) ④ 시드 이벤트 "마지막 봉 N분 전"이 0~3분인지. ⑤ (예정) 2026-11-01 미국 서머타임 해제 — ET가 UTC−5로 바뀌어 프리 KST 18:00·정규 23:30 시작, 한투 주간거래 창 KST 11~17시 — 앱의 KST 고정 세션 창(`autopilotManager` 주간거래 10~16시) 손봐야 함.

## 2026-08-18 — 시드 원천·화면 (데이터 분석 없음, 운영 중 발견)

- **발견**: 한투 분봉조회는 정규장만 제공 → 프리·애프터·주간거래에 4선 꼬임. 토스 c-chart가 세션 무관 연속 봉(오버나이트 포함) 제공, 무인증(`from=`으로 과거 페이징, count≤300).
- **바꾼 것**: `32035d6` 추세 시드 토스 교체, `cf54b27` 차트 화면 분봉도 토스 + 진행 중 봉 컷오프.
- **다음에 볼 것**: 프리·애프터 시간대 토스 `sessionType` 값·연속성.
