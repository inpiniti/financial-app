# 기능: 시세 수신 진단 표시 — WS 상태·실측 워밍업·틱 수신 여부를 카드/헤더에 노출

> 작성일: 2026-07-30 · 분류: **소형** — 기존 scalper 탭·기존 모듈(scalperInstance/scalperManager) 내부 필드 추가 + 표시 교체, 아키텍처/스키마 변경 없음

## 왜
실기기에서 Run 후 "데이터 모으는 중 31/31"에 멈추는 증상 원인(WS 틱 미수신 추정)을 사용자가 UI에서 직접 분간할 수 없음. 워밍업 진행률이 시간 근사치라 실수신과 무관하게 완료로 보이는 오해도 겹침.

## 작업

| 단계 | 작업 | 사용 스킬 | 에이전트 모델 (effort) | 완료 기준 |
|---|---|---|---|---|
| 1 | scalperInstance.ts: View에 tickCount·lastTickAt·sampleCount 추가, Resampler 실측 반영, 발행 스로틀에 편입 | expo-react-native-typescript, tdd | Sonnet (medium, thinking O) | 기존 96개 테스트 green + 신규 1~2개(tickCount/sampleCount 실측) green |
| 2 | scalperManager.ts+types.ts: feed onStatusChange → feedStatus 유지, subscribeFeedStatus 제공 | expo-react-native-typescript | Sonnet (medium, thinking O) | feedStatus가 idle/connecting/open/reconnecting/closed로 전이 |
| 3 | InstanceCard.tsx·scalper.tsx: 워밍업 실측 표시, 진단 줄(미수신/수신중/끊김), 헤더 feedStatus 배지 | toss-design | Sonnet (medium, thinking O) | 실기기 시나리오별(틱 0개/수신중/15초 이상 정체) 문구 확인 |
| 4 | 검증: tsc --noEmit, npm test, expo export --platform ios (dist-test 삭제) | — | Sonnet (medium, thinking O) | 전부 green, dist-test 미잔존 |

## 완료 기준
- `npx tsc --noEmit` green
- `npm test` green (기존 96개 + 신규 포함)
- `npx expo export --platform ios --output-dir dist-test` 성공 후 dist-test 삭제
- core/·kis/ 변경 없음
