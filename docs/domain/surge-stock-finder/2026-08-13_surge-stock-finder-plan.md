# 급등주 찾기(Surge Stock Finder) — 신호 기록 기획

> 작성일 2026-08-13 (당일 논의로 2회 개정: 범위 축소 → 이탈 재정의) · 분류: **중형** (급등 진입 + 하락 이탈 세트 감지 + DB 기록 — 매매 연동 없음)
> 기준: `features/scalper/watchlist.ts`, `features/scalper/feedSlot.ts`, `features/scalper/autopilotManager.ts`, `core/resample/index.ts`, `kis/realtimePrice.ts`, `lib/tossRanking.ts`, `docs/koreainvestment/실시간호가.md`, `supabase/migrations/`
> 하위 문서: [DB 스키마](./2026-08-13_surge-stock-finder-db-schema.md) · [프론트 훅 설계](./2026-08-13_surge-stock-finder-frontend-hooks.md)

## 0. 범위 (논의로 확정)

**진입·눌림목·자동매매 연동은 전부 범위 밖.** 이 기획은 딱 하나만 한다:

> **급등(진입시점)과 그 뒤의 하락(이탈시점)을 세트로** 감지해서 각 순간의 체결가·호가를 **DB에 기록**한다. 쓸만한 신호인지는 나중에 기록을 보고 판단한다.

재정의(2026-08-13 당일 논의): 처음엔 이탈을 "급락(폭주 동반 하락)"으로 정의했으나, 급등 후 **조용히 식는 하락**이 이탈로 안 잡혀 "급락 없이 만료"가 대량 발생하는 문제가 확인됐다. 목적은 진입시점 + 이탈시점의 세트이므로 이탈은 **트레일링 하락**(급등 후 고점 대비 X% 하락 — 폭주 조건 없음)으로 바꾼다. **단독 하락은 기록하지 않는다** — 세트가 아니면 무가치(사용자 확정).

가짜 신호가 섞이는 것을 허용한다 — 오탐 필터링보다 **빠른 발화 + 충실한 기록**이 우선이다. 기록이 곧 오탐율 측정 도구다.

## 1. 기록 스키마 (핵심 산출물)

1행 = 1에피소드(급등 진입 → 하락 이탈). Supabase 테이블 `surge_events` 신설(기존 `approved_users`와 같은 프로젝트, 마이그레이션 추가). `plunge_*` 컬럼명은 이탈(하락 확정) 시점 값이다.

| 컬럼 | 의미 | 기록 시점 |
|---|---|---|
| `ticker` | 종목코드 (+시장구분) | 급등 확정 시 |
| `surge_at` | 급등 시간 | 〃 |
| `surge_price` | 급등 시점 체결가 | 〃 |
| `surge_ask1`, `surge_ask2` | 급등 시점 매도1·2호가 (PASK1/PASK2) | 〃 |
| `plunge_at` | 이탈(하락 확정) 시간 | 이탈 확정 시 |
| `plunge_price` | 급락 시점 체결가 | 〃 |
| `plunge_bid1`, `plunge_bid2` | 급락 시점 매수1·2호가 (PBID1/PBID2) | 〃 |
| `price_change_pct` | 체결가 변동율 = (급락체결가 − 급등체결가) / 급등체결가 | 〃 |
| `l1_change_pct` | 1호가 변동율 = (급락시 매수1호가 − 급등시 매도1호가) / 급등시 매도1호가 | 〃 |
| `l2_change_pct` | 2호가 변동율 = 위와 동일하되 2호가 기준 | 〃 |

- 변동율 정의: **급등 시점 매도호가에 사서 급락 시점 매수호가에 팔았을 때**의 ±% — 스프레드 비용이 포함된 현실적 왕복 수익률.
- 페어링 규칙(이탈 재정의 후): 이탈(EXIT)은 감지기 추적 모드에서만 발화하므로 **항상 같은 종목의 열린 에피소드를 닫는다**. 열린 에피소드 없는 하락은 감지도 기록도 하지 않는다(세트만 기록 — 단독 하락 무가치). 트레일링 이탈이 조용한 하락까지 잡으므로 타임아웃(30분) 만료는 거래가 끊긴 극단 케이스에서만 남는다.
- 2호가 가용성 확인 완료: HDFSASP0 페이로드에 PBID2=17, PASK2=18 존재. 원문 중복 표기로 인덱스가 불확실한 건 3호가 그룹부터이므로 1·2호가는 안전 (`docs/koreainvestment/실시간호가.md:69-70, 93-94`). 현재 파서는 1호가만 소비 중 → 2호가 읽기 추가 필요 (`kis/realtimePrice.ts:130-180`).

## 2. 감지기 설계 — 2단계 (조기경보 → 확정)

후행성 논의 결론: **판정창은 짧게, 기준선은 길게.** 후행성을 만드는 건 판정창이고, 기준선(비교 대상)은 길어도 지연을 만들지 않는다. 기준선이 너무 짧으면(예: 직전 5초) 평소에도 빠른 종목과 방금 빨라진 종목을 구분 못 한다.

### 급등 (SURGE)

| 단계 | 조건 | 지연 | 역할 |
|---|---|---|---|
| 1단계 조기경보 | 틱 레벨: 연속 업틱 + **틱속도 폭주** | ~1초 | **호가 WS 동적 구독 트리거** (기록 준비) |
| 2단계 확정 | 1초 청크 평균가 4개 연속 상승(정배열) AND 최근 10초 내 틱속도 폭주 성립 | 3~5초 | DB 기록 발화 — 이 시점의 체결가·매도1/2호가 스냅샷 |

**틱속도 폭주 = 두 경로의 OR** (2026-08-13 사용자 확정 — 서로의 사각을 메운다):

| 경로 | 조건 | 워밍업 | 잡는 것 / 놓치는 것 |
|---|---|---|---|
| (A) 기준선 배수 | 최근 2초 틱속도 ≥ 롤링 기준선(300초)의 3배 | 필요(60초) | 계단식 점프(1→30틱/초로 뛰어 유지) / 리스트 진입 직후는 침묵 |
| (B) 속도 정배열 | 직전 완결 5초의 초당 틱수가 엄격 증가(1<2<3<4<5) | **불필요** | 점진 가속 — 진입 직후에도 동작. 엄격 증가 5개는 절대 하한(+4틱) 내장 / 계단식 점프는 못 잡음(A가 담당) |

### 이탈 (EXIT — 하락, 급등과 항상 세트, **2단 트레일링**)

급등 확정과 동시에 감지기가 **추적 모드**로 들어간다: 트레일링 고점을 갱신하다가 아래 둘 중 하나에 닿으면 EXIT 확정. 확정 시 체결가·매수1/2호가 스냅샷으로 에피소드를 닫는다. 추적 중에는 새 급등을 내지 않는다(세트 유지).

| 경로 | 조건 | 담당 |
|---|---|---|
| **소프트** | 폭주가 식었고(최근 10초 내 폭주 없음) 고점 대비 `exitSoftDropPct`(1%) 하락 | "급등인 줄 알았는데 힘 빠진" 세트를 3% 되돌림까지 기다리지 않고 빨리 끊는다. 폭주 중의 1% 눌림은 잔파동으로 참는다 |
| **하드** | 고점 대비 `exitDropPct`(3%) 하락 — **속도 무관** | 투매는 속도가 폭발하며 떨어지므로 "식으면"류 조건만으론 폭락 중 이탈을 못 낸다 — 그 구멍을 막는 안전선 |

이탈 경로(soft/hard)는 이벤트 타임라인에 "둔화/급락"으로 남는다 — 기록 리뷰 때 경로별 성적 비교용. 소프트 이탈은 사실상 "모멘텀 사망 판정"이라, 기록되는 왕복 변동율이 곧 "확정 매수 → 모멘텀 소멸 청산" 전략의 성적이 된다.

역정배열 기반 단독 급락 감지는 재정의에서 **폐기** — 세트 밖 하락은 기록하지 않는다.

- 청크는 기존 1초 고정(`managerProvider.tsx:150`) 그대로. 감지기는 `core/surge/` 순수 TS 모듈로 신설, 기존 감지기와 **병렬 부착**(LadderDetector 교체·간섭 없음, 사다리 앵커 리셋 금지).
- STRN·EVOL은 v1 판정 조건에 넣지 않고 **기록만** 해두는 것 고려(추후 필터 후보 분석용) — 스키마 확장 여지로 남김.

## 3. 호가 스냅샷 문제와 해법

급등이 터지는 종목은 보통 호가 구독이 없다(호가는 감시 top3 ∪ 진입중 ∪ 보유만, `autopilotManager.ts:492-518`). 신호 순간의 호가를 기록하려면:

**1단계 조기경보 = 호가 구독 트리거.** 경보가 뜨면 즉시 해당 종목 호가 WS를 붙인다 → 3~5초 뒤 2단계 확정 시점엔 호가 수신 중. 에피소드가 닫히거나(급락 기록) 타임아웃까지 유지 후 해제.

구독 예산 (KIS 한도 41건):

| 용도 | 건수 |
|---|---|
| 체결가 — 리스트 전 종목 (**20 → 30 확대**) | 30 |
| 호가 — 기존 (감시 top3 ∪ 보유, 그리드 1개 기준) | ~4–5 |
| 호가 — 에피소드 동적 구독 (상한 명시, LRU 해제) | ≤ 3 |
| 합계 | ≤ 38 (+상세화면 몫 여유) |

- 리스트 30 확대는 "리스트 밖에서 튀는 종목은 못 본다"는 구조적 사각을 줄이는 효과도 겸한다. 완전 해소는 불가(전 종목 스캔 서버는 기각) — 리스트 밖 종목은 다음 순위 폴링에서야 편입된다.
- `maxConcurrentGrids`를 올려 쓰면 기존 호가 몫이 불어나므로 에피소드 호가 상한과 합산 검사를 코드에 명시할 것.
- 조기경보 남발로 호가 슬롯이 마르면 오래된 경보부터 해제(LRU). 호가 미수신 상태에서 확정이 먼저 오면 호가 컬럼 null로 기록(버리지 않는다).

## 4. 파라미터 초안 (코드 기본값, 설정 화면 노출 없음)

| 파라미터 | 초안값 | 의미 |
|---|---|---|
| `surgeTickRateMultiple` | 3배 | 조기경보 틱속도 배수 (vs 롤링 기준선) |
| `surgeBaselineSec` | 300 | 틱속도 롤링 기준선 길이 |
| `surgeConfirmChunks` | 4 | 정배열 확정에 필요한 연속 상승 1초 청크 수 |
| `exitDropPct` | 3% | 하드 이탈 낙폭 — 트레일링 고점 대비, 폭주 무관 무조건 이탈(투매 안전선) |
| `exitSoftDropPct` | 1% | 소프트 이탈 낙폭 — 폭주가 식은 상태에서 고점 대비 이만큼 하락하면 이탈 |
| `speedAscendSeconds` | 5 | 속도 정배열(B 경로) 판정 초 수 — 완결 5초 틱수 엄격 증가 |
| `episodeTimeoutMin` | 30 | 급등 후 급락 미발생 시 미완결 마감 |
| `episodeQuoteSlots` | 3 | 에피소드 호가 동적 구독 상한 (구현 시 5→3 — 리스트 30 확대 후 41건 예산 재계산) |
| `signalCooldownSec` | 60 | 동일 종목 재발화 쿨다운 (중복 행 방지) |

값들은 전부 기록 데이터를 보고 조정한다 — v1의 목적 자체가 이 값들의 적정치를 찾는 것.

## 5. 작업 분해

| 단계 | 작업 | 산출물 | 완료 기준 |
|---|---|---|---|
| 1 | Supabase `surge_events` 테이블 마이그레이션 + 기록 클라이언트 | `supabase/migrations/`, `lib/surgeLog.ts` | 앱에서 insert/에피소드 close 동작 |
| 2 | `core/surge/` 2단계 감지기 (정배열/역정배열 + 틱속도 기준선) | 순수 TS 모듈 + vitest 틱 재생 테스트 | 급등·급락·노이즈 시나리오 통과, 워밍업 미달 시 무발화 |
| 3 | 호가 파서 2호가 확장 (PBID2/PASK2) | `kis/realtimePrice.ts` 수정 + 테스트 | 실계좌 RAW 덤프로 인덱스 검증 |
| 4 | FeedSlot 병렬 부착 + 조기경보→호가 동적 구독 + 예산 관리 | `feedSlot.ts`, `autopilotManager.ts` | 사다리와 간섭 없음, 41건 한도 초과 없음 |
| 5 | 리스트 20→30 확대 | `watchlist.ts` | 구독 합산 ≤ 40 유지 |
| 6 | 실운영 수집 후 기록 리뷰 | `surge_events` 데이터 | 변동율 분포·오탐 비율 보고 활용 여부 판단 |

## 완료 기준 (이 기획의)

- [x] 범위 확정 — 신호 감지 + DB 기록만, 매매 연동 없음 (§0)
- [x] 기록 스키마·변동율 정의 확정 (§1)
- [x] 2호가 페이로드 가용성 확인 (§1)
- [x] 호가 스냅샷 확보 방법 확정 — 조기경보 트리거 동적 구독 (§3)
- [x] 구현 완료 (2026-08-13) — `core/surge`(감지기) · `features/scalper/surgeRecorder.ts`(에피소드·기록) ·
      `lib/surgeLog.ts`(Supabase 클라이언트) · `supabase/migrations/0004_surge_events.sql` ·
      FeedSlot 병렬 탑재 · 리스트 30 · AutoPilotScreen 신호 패널. vitest 전체 통과.
- [ ] **남은 수동 단계**: ① `0004_surge_events.sql`을 Supabase 대시보드 SQL Editor에서 실행
      ② 실계좌 리허설에서 2호가 인덱스(PBID2=16/PASK2=17, RSYM 오프셋 자동) RAW 덤프 검증
      ③ 1~2주 수집 후 기록 리뷰(§6-6)

## 리스크·미결

- **HDFSCNT0은 지연체결가**(미국 무료 시세) — 신호 시각 자체가 소스 지연을 물고 있다. 기록엔 문제없지만, 데이터 해석 시(특히 "몇 초 만에 잡았나" 평가) 감안할 것. 호가는 나스닥 마켓센터 0분지연 무료.
- 미국 주간거래 세션(KST 10~16시)은 유동성이 얇아 틱속도 기준선이 왜곡될 수 있음 — 세션 전환 시 기준선 리셋 검토 (`daySession.ts`).
- Supabase 기록은 네트워크 실패 시 유실 가능 — v1은 재시도 큐 없이 단순 insert로 시작하되, 유실이 눈에 띄면 로컬 버퍼 추가.
- 토스 비공식 순위 API 폴링 주기 단축은 여전히 보류(차단 리스크) — 리스트 30 확대만 반영.
- **감지 가동 조건 — (a)안 확정(2026-08-13 사용자 결정)**: "자동 트레이딩 시작하기"(`autopilot.start()`)와 함께 켜지고 정지와 함께 꺼진다. 자동매매가 도는 동안의 데이터가 실전 조건과 같아 분석 가치가 높다는 판단. "매매 없이 기록만 모으는" 감시 전용 모드는 필요성이 확인되면 별도 기획.
- 에피소드 운영 규칙(재시작 고아 행 정리, 재급등 무시, 기록 실패 시 감지 지속)은 DB 문서 §2에 확정해 두었다. 단독 하락 기록은 이탈 재정의에서 폐기.
